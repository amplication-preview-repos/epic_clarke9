/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { CopyTrade } from "./CopyTrade";
import { CopyTradeCountArgs } from "./CopyTradeCountArgs";
import { CopyTradeFindManyArgs } from "./CopyTradeFindManyArgs";
import { CopyTradeFindUniqueArgs } from "./CopyTradeFindUniqueArgs";
import { CreateCopyTradeArgs } from "./CreateCopyTradeArgs";
import { UpdateCopyTradeArgs } from "./UpdateCopyTradeArgs";
import { DeleteCopyTradeArgs } from "./DeleteCopyTradeArgs";
import { CopyTradeOrderFindManyArgs } from "../../copyTradeOrder/base/CopyTradeOrderFindManyArgs";
import { CopyTradeOrder } from "../../copyTradeOrder/base/CopyTradeOrder";
import { Setup } from "../../setup/base/Setup";
import { User } from "../../user/base/User";
import { CopyTradeService } from "../copyTrade.service";
@graphql.Resolver(() => CopyTrade)
export class CopyTradeResolverBase {
  constructor(protected readonly service: CopyTradeService) {}

  async _copyTradesMeta(
    @graphql.Args() args: CopyTradeCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [CopyTrade])
  async copyTrades(
    @graphql.Args() args: CopyTradeFindManyArgs
  ): Promise<CopyTrade[]> {
    return this.service.copyTrades(args);
  }

  @graphql.Query(() => CopyTrade, { nullable: true })
  async copyTrade(
    @graphql.Args() args: CopyTradeFindUniqueArgs
  ): Promise<CopyTrade | null> {
    const result = await this.service.copyTrade(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CopyTrade)
  async createCopyTrade(
    @graphql.Args() args: CreateCopyTradeArgs
  ): Promise<CopyTrade> {
    return await this.service.createCopyTrade({
      ...args,
      data: {
        ...args.data,

        setup: args.data.setup
          ? {
              connect: args.data.setup,
            }
          : undefined,

        trader: args.data.trader
          ? {
              connect: args.data.trader,
            }
          : undefined,

        user: args.data.user
          ? {
              connect: args.data.user,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => CopyTrade)
  async updateCopyTrade(
    @graphql.Args() args: UpdateCopyTradeArgs
  ): Promise<CopyTrade | null> {
    try {
      return await this.service.updateCopyTrade({
        ...args,
        data: {
          ...args.data,

          setup: args.data.setup
            ? {
                connect: args.data.setup,
              }
            : undefined,

          trader: args.data.trader
            ? {
                connect: args.data.trader,
              }
            : undefined,

          user: args.data.user
            ? {
                connect: args.data.user,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => CopyTrade)
  async deleteCopyTrade(
    @graphql.Args() args: DeleteCopyTradeArgs
  ): Promise<CopyTrade | null> {
    try {
      return await this.service.deleteCopyTrade(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => [CopyTradeOrder], { name: "copyTradeOrder" })
  async findCopyTradeOrder(
    @graphql.Parent() parent: CopyTrade,
    @graphql.Args() args: CopyTradeOrderFindManyArgs
  ): Promise<CopyTradeOrder[]> {
    const results = await this.service.findCopyTradeOrder(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => Setup, {
    nullable: true,
    name: "setup",
  })
  async getSetup(@graphql.Parent() parent: CopyTrade): Promise<Setup | null> {
    const result = await this.service.getSetup(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "trader",
  })
  async getTrader(@graphql.Parent() parent: CopyTrade): Promise<User | null> {
    const result = await this.service.getTrader(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "user",
  })
  async getUser(@graphql.Parent() parent: CopyTrade): Promise<User | null> {
    const result = await this.service.getUser(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
