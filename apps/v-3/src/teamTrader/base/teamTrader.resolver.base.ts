/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { TeamTrader } from "./TeamTrader";
import { TeamTraderCountArgs } from "./TeamTraderCountArgs";
import { TeamTraderFindManyArgs } from "./TeamTraderFindManyArgs";
import { TeamTraderFindUniqueArgs } from "./TeamTraderFindUniqueArgs";
import { CreateTeamTraderArgs } from "./CreateTeamTraderArgs";
import { UpdateTeamTraderArgs } from "./UpdateTeamTraderArgs";
import { DeleteTeamTraderArgs } from "./DeleteTeamTraderArgs";
import { Team } from "../../team/base/Team";
import { User } from "../../user/base/User";
import { TeamTraderService } from "../teamTrader.service";
@graphql.Resolver(() => TeamTrader)
export class TeamTraderResolverBase {
  constructor(protected readonly service: TeamTraderService) {}

  async _teamTradersMeta(
    @graphql.Args() args: TeamTraderCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [TeamTrader])
  async teamTraders(
    @graphql.Args() args: TeamTraderFindManyArgs
  ): Promise<TeamTrader[]> {
    return this.service.teamTraders(args);
  }

  @graphql.Query(() => TeamTrader, { nullable: true })
  async teamTrader(
    @graphql.Args() args: TeamTraderFindUniqueArgs
  ): Promise<TeamTrader | null> {
    const result = await this.service.teamTrader(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => TeamTrader)
  async createTeamTrader(
    @graphql.Args() args: CreateTeamTraderArgs
  ): Promise<TeamTrader> {
    return await this.service.createTeamTrader({
      ...args,
      data: {
        ...args.data,

        teams: {
          connect: args.data.teams,
        },

        user: {
          connect: args.data.user,
        },
      },
    });
  }

  @graphql.Mutation(() => TeamTrader)
  async updateTeamTrader(
    @graphql.Args() args: UpdateTeamTraderArgs
  ): Promise<TeamTrader | null> {
    try {
      return await this.service.updateTeamTrader({
        ...args,
        data: {
          ...args.data,

          teams: {
            connect: args.data.teams,
          },

          user: {
            connect: args.data.user,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => TeamTrader)
  async deleteTeamTrader(
    @graphql.Args() args: DeleteTeamTraderArgs
  ): Promise<TeamTrader | null> {
    try {
      return await this.service.deleteTeamTrader(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Team, {
    nullable: true,
    name: "teams",
  })
  async getTeams(@graphql.Parent() parent: TeamTrader): Promise<Team | null> {
    const result = await this.service.getTeams(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "user",
  })
  async getUser(@graphql.Parent() parent: TeamTrader): Promise<User | null> {
    const result = await this.service.getUser(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
