/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Clap } from "./Clap";
import { ClapCountArgs } from "./ClapCountArgs";
import { ClapFindManyArgs } from "./ClapFindManyArgs";
import { ClapFindUniqueArgs } from "./ClapFindUniqueArgs";
import { CreateClapArgs } from "./CreateClapArgs";
import { UpdateClapArgs } from "./UpdateClapArgs";
import { DeleteClapArgs } from "./DeleteClapArgs";
import { Setup } from "../../setup/base/Setup";
import { User } from "../../user/base/User";
import { ClapService } from "../clap.service";
@graphql.Resolver(() => Clap)
export class ClapResolverBase {
  constructor(protected readonly service: ClapService) {}

  async _clapsMeta(
    @graphql.Args() args: ClapCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Clap])
  async claps(@graphql.Args() args: ClapFindManyArgs): Promise<Clap[]> {
    return this.service.claps(args);
  }

  @graphql.Query(() => Clap, { nullable: true })
  async clap(@graphql.Args() args: ClapFindUniqueArgs): Promise<Clap | null> {
    const result = await this.service.clap(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Clap)
  async createClap(@graphql.Args() args: CreateClapArgs): Promise<Clap> {
    return await this.service.createClap({
      ...args,
      data: {
        ...args.data,

        setup: args.data.setup
          ? {
              connect: args.data.setup,
            }
          : undefined,

        user: args.data.user
          ? {
              connect: args.data.user,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => Clap)
  async updateClap(@graphql.Args() args: UpdateClapArgs): Promise<Clap | null> {
    try {
      return await this.service.updateClap({
        ...args,
        data: {
          ...args.data,

          setup: args.data.setup
            ? {
                connect: args.data.setup,
              }
            : undefined,

          user: args.data.user
            ? {
                connect: args.data.user,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Clap)
  async deleteClap(@graphql.Args() args: DeleteClapArgs): Promise<Clap | null> {
    try {
      return await this.service.deleteClap(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Setup, {
    nullable: true,
    name: "setup",
  })
  async getSetup(@graphql.Parent() parent: Clap): Promise<Setup | null> {
    const result = await this.service.getSetup(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "user",
  })
  async getUser(@graphql.Parent() parent: Clap): Promise<User | null> {
    const result = await this.service.getUser(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
