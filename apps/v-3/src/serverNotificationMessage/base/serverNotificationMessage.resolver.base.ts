/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { ServerNotificationMessage } from "./ServerNotificationMessage";
import { ServerNotificationMessageCountArgs } from "./ServerNotificationMessageCountArgs";
import { ServerNotificationMessageFindManyArgs } from "./ServerNotificationMessageFindManyArgs";
import { ServerNotificationMessageFindUniqueArgs } from "./ServerNotificationMessageFindUniqueArgs";
import { CreateServerNotificationMessageArgs } from "./CreateServerNotificationMessageArgs";
import { UpdateServerNotificationMessageArgs } from "./UpdateServerNotificationMessageArgs";
import { DeleteServerNotificationMessageArgs } from "./DeleteServerNotificationMessageArgs";
import { ServerNotificationMessageService } from "../serverNotificationMessage.service";
@graphql.Resolver(() => ServerNotificationMessage)
export class ServerNotificationMessageResolverBase {
  constructor(protected readonly service: ServerNotificationMessageService) {}

  async _serverNotificationMessagesMeta(
    @graphql.Args() args: ServerNotificationMessageCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [ServerNotificationMessage])
  async serverNotificationMessages(
    @graphql.Args() args: ServerNotificationMessageFindManyArgs
  ): Promise<ServerNotificationMessage[]> {
    return this.service.serverNotificationMessages(args);
  }

  @graphql.Query(() => ServerNotificationMessage, { nullable: true })
  async serverNotificationMessage(
    @graphql.Args() args: ServerNotificationMessageFindUniqueArgs
  ): Promise<ServerNotificationMessage | null> {
    const result = await this.service.serverNotificationMessage(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => ServerNotificationMessage)
  async createServerNotificationMessage(
    @graphql.Args() args: CreateServerNotificationMessageArgs
  ): Promise<ServerNotificationMessage> {
    return await this.service.createServerNotificationMessage({
      ...args,
      data: args.data,
    });
  }

  @graphql.Mutation(() => ServerNotificationMessage)
  async updateServerNotificationMessage(
    @graphql.Args() args: UpdateServerNotificationMessageArgs
  ): Promise<ServerNotificationMessage | null> {
    try {
      return await this.service.updateServerNotificationMessage({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => ServerNotificationMessage)
  async deleteServerNotificationMessage(
    @graphql.Args() args: DeleteServerNotificationMessageArgs
  ): Promise<ServerNotificationMessage | null> {
    try {
      return await this.service.deleteServerNotificationMessage(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
