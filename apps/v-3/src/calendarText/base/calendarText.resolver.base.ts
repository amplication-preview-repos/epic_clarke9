/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { CalendarText } from "./CalendarText";
import { CalendarTextCountArgs } from "./CalendarTextCountArgs";
import { CalendarTextFindManyArgs } from "./CalendarTextFindManyArgs";
import { CalendarTextFindUniqueArgs } from "./CalendarTextFindUniqueArgs";
import { CreateCalendarTextArgs } from "./CreateCalendarTextArgs";
import { UpdateCalendarTextArgs } from "./UpdateCalendarTextArgs";
import { DeleteCalendarTextArgs } from "./DeleteCalendarTextArgs";
import { Calendar } from "../../calendar/base/Calendar";
import { CalendarTextService } from "../calendarText.service";
@graphql.Resolver(() => CalendarText)
export class CalendarTextResolverBase {
  constructor(protected readonly service: CalendarTextService) {}

  async _calendarTextsMeta(
    @graphql.Args() args: CalendarTextCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [CalendarText])
  async calendarTexts(
    @graphql.Args() args: CalendarTextFindManyArgs
  ): Promise<CalendarText[]> {
    return this.service.calendarTexts(args);
  }

  @graphql.Query(() => CalendarText, { nullable: true })
  async calendarText(
    @graphql.Args() args: CalendarTextFindUniqueArgs
  ): Promise<CalendarText | null> {
    const result = await this.service.calendarText(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CalendarText)
  async createCalendarText(
    @graphql.Args() args: CreateCalendarTextArgs
  ): Promise<CalendarText> {
    return await this.service.createCalendarText({
      ...args,
      data: {
        ...args.data,

        calendar: {
          connect: args.data.calendar,
        },
      },
    });
  }

  @graphql.Mutation(() => CalendarText)
  async updateCalendarText(
    @graphql.Args() args: UpdateCalendarTextArgs
  ): Promise<CalendarText | null> {
    try {
      return await this.service.updateCalendarText({
        ...args,
        data: {
          ...args.data,

          calendar: {
            connect: args.data.calendar,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => CalendarText)
  async deleteCalendarText(
    @graphql.Args() args: DeleteCalendarTextArgs
  ): Promise<CalendarText | null> {
    try {
      return await this.service.deleteCalendarText(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Calendar, {
    nullable: true,
    name: "calendar",
  })
  async getCalendar(
    @graphql.Parent() parent: CalendarText
  ): Promise<Calendar | null> {
    const result = await this.service.getCalendar(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
