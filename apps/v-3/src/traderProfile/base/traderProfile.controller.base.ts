/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TraderProfileService } from "../traderProfile.service";
import { TraderProfileCreateInput } from "./TraderProfileCreateInput";
import { TraderProfile } from "./TraderProfile";
import { TraderProfileFindManyArgs } from "./TraderProfileFindManyArgs";
import { TraderProfileWhereUniqueInput } from "./TraderProfileWhereUniqueInput";
import { TraderProfileUpdateInput } from "./TraderProfileUpdateInput";

export class TraderProfileControllerBase {
  constructor(protected readonly service: TraderProfileService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TraderProfile })
  async createTraderProfile(
    @common.Body() data: TraderProfileCreateInput
  ): Promise<TraderProfile> {
    return await this.service.createTraderProfile({
      data: {
        ...data,

        trader: {
          connect: data.trader,
        },
      },
      select: {
        content: true,
        coverImage: true,
        createdAt: true,
        createdById: true,
        id: true,
        isDeleted: true,
        links: true,

        trader: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
        updatedById: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [TraderProfile] })
  @ApiNestedQuery(TraderProfileFindManyArgs)
  async traderProfiles(
    @common.Req() request: Request
  ): Promise<TraderProfile[]> {
    const args = plainToClass(TraderProfileFindManyArgs, request.query);
    return this.service.traderProfiles({
      ...args,
      select: {
        content: true,
        coverImage: true,
        createdAt: true,
        createdById: true,
        id: true,
        isDeleted: true,
        links: true,

        trader: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
        updatedById: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TraderProfile })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async traderProfile(
    @common.Param() params: TraderProfileWhereUniqueInput
  ): Promise<TraderProfile | null> {
    const result = await this.service.traderProfile({
      where: params,
      select: {
        content: true,
        coverImage: true,
        createdAt: true,
        createdById: true,
        id: true,
        isDeleted: true,
        links: true,

        trader: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
        updatedById: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TraderProfile })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateTraderProfile(
    @common.Param() params: TraderProfileWhereUniqueInput,
    @common.Body() data: TraderProfileUpdateInput
  ): Promise<TraderProfile | null> {
    try {
      return await this.service.updateTraderProfile({
        where: params,
        data: {
          ...data,

          trader: {
            connect: data.trader,
          },
        },
        select: {
          content: true,
          coverImage: true,
          createdAt: true,
          createdById: true,
          id: true,
          isDeleted: true,
          links: true,

          trader: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
          updatedById: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TraderProfile })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteTraderProfile(
    @common.Param() params: TraderProfileWhereUniqueInput
  ): Promise<TraderProfile | null> {
    try {
      return await this.service.deleteTraderProfile({
        where: params,
        select: {
          content: true,
          coverImage: true,
          createdAt: true,
          createdById: true,
          id: true,
          isDeleted: true,
          links: true,

          trader: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
          updatedById: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
