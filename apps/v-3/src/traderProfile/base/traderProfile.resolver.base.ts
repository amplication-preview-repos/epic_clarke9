/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { TraderProfile } from "./TraderProfile";
import { TraderProfileCountArgs } from "./TraderProfileCountArgs";
import { TraderProfileFindManyArgs } from "./TraderProfileFindManyArgs";
import { TraderProfileFindUniqueArgs } from "./TraderProfileFindUniqueArgs";
import { CreateTraderProfileArgs } from "./CreateTraderProfileArgs";
import { UpdateTraderProfileArgs } from "./UpdateTraderProfileArgs";
import { DeleteTraderProfileArgs } from "./DeleteTraderProfileArgs";
import { User } from "../../user/base/User";
import { TraderProfileService } from "../traderProfile.service";
@graphql.Resolver(() => TraderProfile)
export class TraderProfileResolverBase {
  constructor(protected readonly service: TraderProfileService) {}

  async _traderProfilesMeta(
    @graphql.Args() args: TraderProfileCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [TraderProfile])
  async traderProfiles(
    @graphql.Args() args: TraderProfileFindManyArgs
  ): Promise<TraderProfile[]> {
    return this.service.traderProfiles(args);
  }

  @graphql.Query(() => TraderProfile, { nullable: true })
  async traderProfile(
    @graphql.Args() args: TraderProfileFindUniqueArgs
  ): Promise<TraderProfile | null> {
    const result = await this.service.traderProfile(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => TraderProfile)
  async createTraderProfile(
    @graphql.Args() args: CreateTraderProfileArgs
  ): Promise<TraderProfile> {
    return await this.service.createTraderProfile({
      ...args,
      data: {
        ...args.data,

        trader: {
          connect: args.data.trader,
        },
      },
    });
  }

  @graphql.Mutation(() => TraderProfile)
  async updateTraderProfile(
    @graphql.Args() args: UpdateTraderProfileArgs
  ): Promise<TraderProfile | null> {
    try {
      return await this.service.updateTraderProfile({
        ...args,
        data: {
          ...args.data,

          trader: {
            connect: args.data.trader,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => TraderProfile)
  async deleteTraderProfile(
    @graphql.Args() args: DeleteTraderProfileArgs
  ): Promise<TraderProfile | null> {
    try {
      return await this.service.deleteTraderProfile(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "trader",
  })
  async getTrader(
    @graphql.Parent() parent: TraderProfile
  ): Promise<User | null> {
    const result = await this.service.getTrader(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
