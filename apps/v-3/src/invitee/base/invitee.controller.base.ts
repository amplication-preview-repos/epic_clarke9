/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { InviteeService } from "../invitee.service";
import { InviteeCreateInput } from "./InviteeCreateInput";
import { Invitee } from "./Invitee";
import { InviteeFindManyArgs } from "./InviteeFindManyArgs";
import { InviteeWhereUniqueInput } from "./InviteeWhereUniqueInput";
import { InviteeUpdateInput } from "./InviteeUpdateInput";

export class InviteeControllerBase {
  constructor(protected readonly service: InviteeService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Invitee })
  async createInvitee(
    @common.Body() data: InviteeCreateInput
  ): Promise<Invitee> {
    return await this.service.createInvitee({
      data: data,
      select: {
        activatedAt: true,
        id: true,
        market: true,
        uid: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Invitee] })
  @ApiNestedQuery(InviteeFindManyArgs)
  async invitees(@common.Req() request: Request): Promise<Invitee[]> {
    const args = plainToClass(InviteeFindManyArgs, request.query);
    return this.service.invitees({
      ...args,
      select: {
        activatedAt: true,
        id: true,
        market: true,
        uid: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Invitee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async invitee(
    @common.Param() params: InviteeWhereUniqueInput
  ): Promise<Invitee | null> {
    const result = await this.service.invitee({
      where: params,
      select: {
        activatedAt: true,
        id: true,
        market: true,
        uid: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Invitee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateInvitee(
    @common.Param() params: InviteeWhereUniqueInput,
    @common.Body() data: InviteeUpdateInput
  ): Promise<Invitee | null> {
    try {
      return await this.service.updateInvitee({
        where: params,
        data: data,
        select: {
          activatedAt: true,
          id: true,
          market: true,
          uid: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Invitee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteInvitee(
    @common.Param() params: InviteeWhereUniqueInput
  ): Promise<Invitee | null> {
    try {
      return await this.service.deleteInvitee({
        where: params,
        select: {
          activatedAt: true,
          id: true,
          market: true,
          uid: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
