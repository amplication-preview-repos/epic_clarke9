/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Invitee } from "./Invitee";
import { InviteeCountArgs } from "./InviteeCountArgs";
import { InviteeFindManyArgs } from "./InviteeFindManyArgs";
import { InviteeFindUniqueArgs } from "./InviteeFindUniqueArgs";
import { CreateInviteeArgs } from "./CreateInviteeArgs";
import { UpdateInviteeArgs } from "./UpdateInviteeArgs";
import { DeleteInviteeArgs } from "./DeleteInviteeArgs";
import { InviteeService } from "../invitee.service";
@graphql.Resolver(() => Invitee)
export class InviteeResolverBase {
  constructor(protected readonly service: InviteeService) {}

  async _inviteesMeta(
    @graphql.Args() args: InviteeCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Invitee])
  async invitees(
    @graphql.Args() args: InviteeFindManyArgs
  ): Promise<Invitee[]> {
    return this.service.invitees(args);
  }

  @graphql.Query(() => Invitee, { nullable: true })
  async invitee(
    @graphql.Args() args: InviteeFindUniqueArgs
  ): Promise<Invitee | null> {
    const result = await this.service.invitee(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Invitee)
  async createInvitee(
    @graphql.Args() args: CreateInviteeArgs
  ): Promise<Invitee> {
    return await this.service.createInvitee({
      ...args,
      data: args.data,
    });
  }

  @graphql.Mutation(() => Invitee)
  async updateInvitee(
    @graphql.Args() args: UpdateInviteeArgs
  ): Promise<Invitee | null> {
    try {
      return await this.service.updateInvitee({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Invitee)
  async deleteInvitee(
    @graphql.Args() args: DeleteInviteeArgs
  ): Promise<Invitee | null> {
    try {
      return await this.service.deleteInvitee(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
