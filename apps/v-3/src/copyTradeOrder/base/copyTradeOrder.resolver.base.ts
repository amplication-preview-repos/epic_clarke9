/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { CopyTradeOrder } from "./CopyTradeOrder";
import { CopyTradeOrderCountArgs } from "./CopyTradeOrderCountArgs";
import { CopyTradeOrderFindManyArgs } from "./CopyTradeOrderFindManyArgs";
import { CopyTradeOrderFindUniqueArgs } from "./CopyTradeOrderFindUniqueArgs";
import { CreateCopyTradeOrderArgs } from "./CreateCopyTradeOrderArgs";
import { UpdateCopyTradeOrderArgs } from "./UpdateCopyTradeOrderArgs";
import { DeleteCopyTradeOrderArgs } from "./DeleteCopyTradeOrderArgs";
import { CopyTrade } from "../../copyTrade/base/CopyTrade";
import { Setup } from "../../setup/base/Setup";
import { User } from "../../user/base/User";
import { CopyTradeOrderService } from "../copyTradeOrder.service";
@graphql.Resolver(() => CopyTradeOrder)
export class CopyTradeOrderResolverBase {
  constructor(protected readonly service: CopyTradeOrderService) {}

  async _copyTradeOrdersMeta(
    @graphql.Args() args: CopyTradeOrderCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [CopyTradeOrder])
  async copyTradeOrders(
    @graphql.Args() args: CopyTradeOrderFindManyArgs
  ): Promise<CopyTradeOrder[]> {
    return this.service.copyTradeOrders(args);
  }

  @graphql.Query(() => CopyTradeOrder, { nullable: true })
  async copyTradeOrder(
    @graphql.Args() args: CopyTradeOrderFindUniqueArgs
  ): Promise<CopyTradeOrder | null> {
    const result = await this.service.copyTradeOrder(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CopyTradeOrder)
  async createCopyTradeOrder(
    @graphql.Args() args: CreateCopyTradeOrderArgs
  ): Promise<CopyTradeOrder> {
    return await this.service.createCopyTradeOrder({
      ...args,
      data: {
        ...args.data,

        copyTrades: args.data.copyTrades
          ? {
              connect: args.data.copyTrades,
            }
          : undefined,

        setup: args.data.setup
          ? {
              connect: args.data.setup,
            }
          : undefined,

        user: args.data.user
          ? {
              connect: args.data.user,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => CopyTradeOrder)
  async updateCopyTradeOrder(
    @graphql.Args() args: UpdateCopyTradeOrderArgs
  ): Promise<CopyTradeOrder | null> {
    try {
      return await this.service.updateCopyTradeOrder({
        ...args,
        data: {
          ...args.data,

          copyTrades: args.data.copyTrades
            ? {
                connect: args.data.copyTrades,
              }
            : undefined,

          setup: args.data.setup
            ? {
                connect: args.data.setup,
              }
            : undefined,

          user: args.data.user
            ? {
                connect: args.data.user,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => CopyTradeOrder)
  async deleteCopyTradeOrder(
    @graphql.Args() args: DeleteCopyTradeOrderArgs
  ): Promise<CopyTradeOrder | null> {
    try {
      return await this.service.deleteCopyTradeOrder(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => CopyTrade, {
    nullable: true,
    name: "copyTrades",
  })
  async getCopyTrades(
    @graphql.Parent() parent: CopyTradeOrder
  ): Promise<CopyTrade | null> {
    const result = await this.service.getCopyTrades(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => Setup, {
    nullable: true,
    name: "setup",
  })
  async getSetup(
    @graphql.Parent() parent: CopyTradeOrder
  ): Promise<Setup | null> {
    const result = await this.service.getSetup(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "user",
  })
  async getUser(
    @graphql.Parent() parent: CopyTradeOrder
  ): Promise<User | null> {
    const result = await this.service.getUser(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
