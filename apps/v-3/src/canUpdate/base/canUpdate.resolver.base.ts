/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { CanUpdate } from "./CanUpdate";
import { CanUpdateCountArgs } from "./CanUpdateCountArgs";
import { CanUpdateFindManyArgs } from "./CanUpdateFindManyArgs";
import { CanUpdateFindUniqueArgs } from "./CanUpdateFindUniqueArgs";
import { CreateCanUpdateArgs } from "./CreateCanUpdateArgs";
import { UpdateCanUpdateArgs } from "./UpdateCanUpdateArgs";
import { DeleteCanUpdateArgs } from "./DeleteCanUpdateArgs";
import { CanUpdateService } from "../canUpdate.service";
@graphql.Resolver(() => CanUpdate)
export class CanUpdateResolverBase {
  constructor(protected readonly service: CanUpdateService) {}

  async _canUpdatesMeta(
    @graphql.Args() args: CanUpdateCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [CanUpdate])
  async canUpdates(
    @graphql.Args() args: CanUpdateFindManyArgs
  ): Promise<CanUpdate[]> {
    return this.service.canUpdates(args);
  }

  @graphql.Query(() => CanUpdate, { nullable: true })
  async canUpdate(
    @graphql.Args() args: CanUpdateFindUniqueArgs
  ): Promise<CanUpdate | null> {
    const result = await this.service.canUpdate(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CanUpdate)
  async createCanUpdate(
    @graphql.Args() args: CreateCanUpdateArgs
  ): Promise<CanUpdate> {
    return await this.service.createCanUpdate({
      ...args,
      data: args.data,
    });
  }

  @graphql.Mutation(() => CanUpdate)
  async updateCanUpdate(
    @graphql.Args() args: UpdateCanUpdateArgs
  ): Promise<CanUpdate | null> {
    try {
      return await this.service.updateCanUpdate({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => CanUpdate)
  async deleteCanUpdate(
    @graphql.Args() args: DeleteCanUpdateArgs
  ): Promise<CanUpdate | null> {
    try {
      return await this.service.deleteCanUpdate(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
