/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { NewsText } from "./NewsText";
import { NewsTextCountArgs } from "./NewsTextCountArgs";
import { NewsTextFindManyArgs } from "./NewsTextFindManyArgs";
import { NewsTextFindUniqueArgs } from "./NewsTextFindUniqueArgs";
import { CreateNewsTextArgs } from "./CreateNewsTextArgs";
import { UpdateNewsTextArgs } from "./UpdateNewsTextArgs";
import { DeleteNewsTextArgs } from "./DeleteNewsTextArgs";
import { News } from "../../news/base/News";
import { NewsTextService } from "../newsText.service";
@graphql.Resolver(() => NewsText)
export class NewsTextResolverBase {
  constructor(protected readonly service: NewsTextService) {}

  async _newsTextsMeta(
    @graphql.Args() args: NewsTextCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [NewsText])
  async newsTexts(
    @graphql.Args() args: NewsTextFindManyArgs
  ): Promise<NewsText[]> {
    return this.service.newsTexts(args);
  }

  @graphql.Query(() => NewsText, { nullable: true })
  async newsText(
    @graphql.Args() args: NewsTextFindUniqueArgs
  ): Promise<NewsText | null> {
    const result = await this.service.newsText(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => NewsText)
  async createNewsText(
    @graphql.Args() args: CreateNewsTextArgs
  ): Promise<NewsText> {
    return await this.service.createNewsText({
      ...args,
      data: {
        ...args.data,

        news: {
          connect: args.data.news,
        },
      },
    });
  }

  @graphql.Mutation(() => NewsText)
  async updateNewsText(
    @graphql.Args() args: UpdateNewsTextArgs
  ): Promise<NewsText | null> {
    try {
      return await this.service.updateNewsText({
        ...args,
        data: {
          ...args.data,

          news: {
            connect: args.data.news,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => NewsText)
  async deleteNewsText(
    @graphql.Args() args: DeleteNewsTextArgs
  ): Promise<NewsText | null> {
    try {
      return await this.service.deleteNewsText(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => News, {
    nullable: true,
    name: "news",
  })
  async getNews(@graphql.Parent() parent: NewsText): Promise<News | null> {
    const result = await this.service.getNews(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
