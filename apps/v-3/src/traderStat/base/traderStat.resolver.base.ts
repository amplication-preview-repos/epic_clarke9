/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { TraderStat } from "./TraderStat";
import { TraderStatCountArgs } from "./TraderStatCountArgs";
import { TraderStatFindManyArgs } from "./TraderStatFindManyArgs";
import { TraderStatFindUniqueArgs } from "./TraderStatFindUniqueArgs";
import { CreateTraderStatArgs } from "./CreateTraderStatArgs";
import { UpdateTraderStatArgs } from "./UpdateTraderStatArgs";
import { DeleteTraderStatArgs } from "./DeleteTraderStatArgs";
import { User } from "../../user/base/User";
import { TraderStatService } from "../traderStat.service";
@graphql.Resolver(() => TraderStat)
export class TraderStatResolverBase {
  constructor(protected readonly service: TraderStatService) {}

  async _traderStatsMeta(
    @graphql.Args() args: TraderStatCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [TraderStat])
  async traderStats(
    @graphql.Args() args: TraderStatFindManyArgs
  ): Promise<TraderStat[]> {
    return this.service.traderStats(args);
  }

  @graphql.Query(() => TraderStat, { nullable: true })
  async traderStat(
    @graphql.Args() args: TraderStatFindUniqueArgs
  ): Promise<TraderStat | null> {
    const result = await this.service.traderStat(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => TraderStat)
  async createTraderStat(
    @graphql.Args() args: CreateTraderStatArgs
  ): Promise<TraderStat> {
    return await this.service.createTraderStat({
      ...args,
      data: {
        ...args.data,

        trader: {
          connect: args.data.trader,
        },
      },
    });
  }

  @graphql.Mutation(() => TraderStat)
  async updateTraderStat(
    @graphql.Args() args: UpdateTraderStatArgs
  ): Promise<TraderStat | null> {
    try {
      return await this.service.updateTraderStat({
        ...args,
        data: {
          ...args.data,

          trader: {
            connect: args.data.trader,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => TraderStat)
  async deleteTraderStat(
    @graphql.Args() args: DeleteTraderStatArgs
  ): Promise<TraderStat | null> {
    try {
      return await this.service.deleteTraderStat(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "trader",
  })
  async getTrader(@graphql.Parent() parent: TraderStat): Promise<User | null> {
    const result = await this.service.getTrader(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
